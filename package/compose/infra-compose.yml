services:
  nginx:
    image: nginx:latest
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - runtime/nginx/conf/:/etc/nginx/conf.d/:ro
      - runtime/certbot/www:/var/www/certbot/:ro
      - runtime/certbot/conf/:/etc/nginx/ssl/:ro
  certbot:
    image: certbot/certbot:latest
    volumes:
      - runtime/certbot/www/:/var/www/certbot/:rw
      - runtime/certbot/conf/:/etc/letsencrypt/:rw
